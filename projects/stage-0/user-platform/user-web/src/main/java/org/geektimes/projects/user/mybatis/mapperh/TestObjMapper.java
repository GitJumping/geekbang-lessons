package org.geektimes.projects.user.mybatis.mapperh;

import org.geektimes.projects.user.mybatis.entityh.TestObj;

import java.util.List;

public interface TestObjMapper {
    List<TestObj> getAll();
}
